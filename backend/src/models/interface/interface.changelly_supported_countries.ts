export interface ChangellySupportedCountriesInterface {
    id: number,
    code: string | null,
    name: string | null,
    status: number | null,
    state_code: string | null,
    created_at: Date,
    updated_at: Date
}